language: generic
services: docker
script: docker run --volume=$(pwd):/app --workdir=/app coala/base bash -c 'pip3 install -U "hg+https://bitbucket.org/jayvdb/pygments-main@relative-tabs-test" "git+https://github.com/jayvdb/coala@ilab"; coala-ci'
